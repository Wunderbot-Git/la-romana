# Ryder Cup Par 00

Golf tournament scoring application.

## Auto-Deploy

Pushes to `main` automatically trigger a Cloud Build that re-deploys both API and Web. See `DEPLOY.md` for the underlying setup.
